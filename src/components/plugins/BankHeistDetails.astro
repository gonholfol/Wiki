---
interface Props {
  plugin: {
    details: {
      schedule: string;
      floors: number;
      doors: number;
      safes: number;
      guards: number;
      rewards: {
        workerBonus: string;
        workerKill: string;
      };
      duration: string;
      features: string[];
      miniEvents: Array<{
        name: string;
        chance?: string;
        trigger?: string;
        description: string;
      }>;
      restrictions: string[];
      update: string;
    };
  };
}

const { plugin } = Astro.props;
---

<div class="mt-6 space-y-6">
  <div class="bg-rust-dark/30 rounded-lg p-6">
    <h4 class="text-lg font-semibold text-rust-pink mb-4">Введение</h4>
    <p class="text-gray-300 mb-4">
      Cobalt банк - это эксклюзивный ивент, доступный только на нашем проекте. Он сочетает в себе элементы из различных игр, от взлома электрических схем как в Watch Dogs 2 до бурения сейфов как в GTA 5.
    </p>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
      <div class="bg-rust-dark/20 p-4 rounded-lg">
        <h5 class="font-medium mb-2">Основная информация</h5>
        <ul class="space-y-2 text-gray-300">
          <li>🕐 Время проведения: {plugin.details.schedule}</li>
          <li>⏳ Длительность: {plugin.details.duration}</li>
          <li>🏢 Этажей: {plugin.details.floors}</li>
          <li>🚪 Дверей: {plugin.details.doors}</li>
          <li>🔒 Сейфов: {plugin.details.safes}</li>
          <li>👥 Охранников: {plugin.details.guards}</li>
        </ul>
      </div>
      <div class="bg-rust-dark/20 p-4 rounded-lg">
        <h5 class="font-medium mb-2">Награды</h5>
        <ul class="space-y-2 text-gray-300">
          <li>✅ {plugin.details.rewards.workerBonus}</li>
          <li>💀 {plugin.details.rewards.workerKill}</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="bg-rust-dark/30 rounded-lg p-6">
    <h4 class="text-lg font-semibold text-rust-pink mb-4">Особенности банка</h4>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      {plugin.details.features.map(feature => (
        <div class="bg-rust-dark/20 p-3 rounded-lg">
          <p class="text-gray-300">✓ {feature}</p>
        </div>
      ))}
    </div>
  </div>

  <div class="bg-rust-dark/30 rounded-lg p-6">
    <h4 class="text-lg font-semibold text-rust-pink mb-4">Мини-ивенты</h4>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      {plugin.details.miniEvents.map(event => (
        <div class="bg-rust-dark/20 p-4 rounded-lg">
          <h5 class="font-medium mb-2">{event.name}</h5>
          {event.chance && <p class="text-sm text-gray-400">Шанс появления: {event.chance}</p>}
          {event.trigger && <p class="text-sm text-gray-400">Условие: {event.trigger}</p>}
          <p class="text-gray-300 mt-2">{event.description}</p>
        </div>
      ))}
    </div>
  </div>

  <div class="bg-rust-dark/30 rounded-lg p-6">
    <h4 class="text-lg font-semibold text-rust-pink mb-4">Ограничения</h4>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      {plugin.details.restrictions.map(restriction => (
        <div class="flex items-start space-x-2">
          <span class="text-red-500">⚠️</span>
          <p class="text-gray-300">{restriction}</p>
        </div>
      ))}
    </div>
  </div>

  <div class="bg-rust-dark/30 rounded-lg p-6">
    <h4 class="text-lg font-semibold text-rust-pink mb-4">Механики взлома</h4>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div class="bg-rust-dark/20 p-4 rounded-lg">
        <h5 class="font-medium mb-2">Взлом дверей</h5>
        <ul class="list-disc list-inside text-gray-300 space-y-2">
          <li>Используйте ноутбуки на стенах</li>
          <li>Соедините электрическую схему</li>
          <li>Поворачивайте соединители левой кнопкой мыши</li>
          <li>Можно закрыть дверь, разорвав схему</li>
        </ul>
      </div>
      <div class="bg-rust-dark/20 p-4 rounded-lg">
        <h5 class="font-medium mb-2">Взлом сейфов</h5>
        <ul class="list-disc list-inside text-gray-300 space-y-2">
          <li>Нажмите E для взаимодействия</li>
          <li>Используйте кнопку "сверлить"</li>
          <li>Следите за температурой бура</li>
          <li>При перегреве теряется прогресс</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="bg-rust-dark/30 rounded-lg p-6">
    <h4 class="text-lg font-semibold text-rust-pink mb-4">Последнее обновление</h4>
    <p class="text-gray-300">
      Версия от {plugin.details.update}. Мы продолжаем улучшать и дополнять механики ивента!
    </p>
  </div>
</div>