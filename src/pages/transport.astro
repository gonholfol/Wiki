---
import Layout from '../layouts/Layout.astro';

const vehicles = [
  {
    name: "Автомобиль",
    icon: "🚗",
    description: "Базовый транспорт для передвижения по карте",
    stats: {
      speed: 80,
      fuel: 100,
      seats: 4
    }
  },
  {
    name: "Вертолет",
    icon: "🚁",
    description: "Воздушный транспорт для быстрого перемещения",
    stats: {
      speed: 120,
      fuel: 200,
      seats: 2
    }
  },
  {
    name: "Лодка",
    icon: "🚤",
    description: "Водный транспорт для морских путешествий",
    stats: {
      speed: 60,
      fuel: 150,
      seats: 3
    }
  }
];
---

<Layout title="Транспорт - Ultimate Rust Wiki">
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="bg-rust-gray rounded-lg p-8">
      <h1 class="text-3xl font-bold mb-6">Транспорт</h1>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
        {vehicles.map(vehicle => (
          <div class="bg-rust-dark/50 rounded-lg p-6">
            <div class="flex items-center space-x-3 mb-4">
              <span class="text-3xl">{vehicle.icon}</span>
              <h3 class="text-xl font-semibold">{vehicle.name}</h3>
            </div>
            <p class="text-gray-300 mb-4">{vehicle.description}</p>
            <div class="space-y-2">
              <div class="flex justify-between">
                <span class="text-gray-400">Скорость:</span>
                <span>{vehicle.stats.speed} км/ч</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-400">Топливо:</span>
                <span>{vehicle.stats.fuel} л</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-400">Места:</span>
                <span>{vehicle.stats.seats}</span>
              </div>
            </div>
          </div>
        ))}
      </div>

      <div class="prose prose-invert max-w-none">
        <h2 class="text-2xl font-semibold mb-4">Правила использования транспорта</h2>
        <ul class="list-disc list-inside space-y-2 text-gray-300">
          <li>Транспорт можно приобрести в специальных магазинах</li>
          <li>Необходимо заправлять транспорт топливом</li>
          <li>При повреждении требуется ремонт</li>
          <li>Парковка разрешена только в специальных зонах</li>
        </ul>
      </div>
    </div>
  </main>
</Layout>